colors:
  normal:
    black: "{{ color0 }}"
    red: "{{ color1 }}"
    green: "{{ color2 }}"
    yellow: "{{ color3 }}"
    blue: "{{ color4 }}" 
    magenta: "{{ color5 }}"
    cyan: "{{ color6 }}"
    white: "{{ color7 }}"
  bright:
    black: "{{ color8 }}"
    red: "{{ color9 }}"
    green: "{{ color10 }}"
    yellow: "{{ color11 }}"
    blue: "{{ color12 }}" 
    magenta: "{{ color13 }}"
    cyan: "{{ color14 }}"
    white: "{{ color15 }}"
  primary:
    background: "{{ bg_primary }}"
    foreground: "{{ bg_primary_alt }}"
font:
  bold:
    family: MesloLGS NF
    style: Bold
  bold_italic:
    family: MesloLGS NF
    style: Bold Italic
  italic:
    family: MesloLGS NF
    style: Italic
  normal:
    family: MesloLGS NF
    style: Regular
window:
  padding:
    x: 4
    y: 4
